# Added as each step lands, so this file never lists something the code does not use.
pdfplumber==0.11.4

# Embeddings, run locally through ONNX. No PyTorch, no API key, no network at query
# time — which is what makes "CI green with no key and no network" achievable.
fastembed==0.8.0

# Vector store. Embedded mode (a local folder) by default, so no server is required;
# set QDRANT_URL to point at the container in docker-compose.yml instead.
qdrant-client==1.19.1

# HTTP layer for POST /ask. FastAPI gives request validation and an OpenAPI page for
# free, which is most of what a reviewer needs to try the thing without reading code.
fastapi==0.141.1
uvicorn==0.53.0
